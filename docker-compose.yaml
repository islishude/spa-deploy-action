version: '3.8'

services:
  localstack:
    image: localstack/localstack
    ports:
      - 4566:4566
    healthcheck:
      test: ['CMD-SHELL', 'awslocal s3 ls']
      interval: 1s
      timeout: 1s
      retries: 5
      start_period: 3s
